name: Still Alive

on:
  schedule:
    - cron: '*/5 * * * *'   # jalan tiap 5 menit
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping /ping
        run: curl -s https://pixeldev-production.up.railway.app/ping